---
title: "输入校验测试（4.7）"
parent: "OWASP WSTG Web 安全测试指南知识库"
nav_order: 8
---

# WSTG 实战｜输入校验测试（4.7）

"用户输入不可信"是安全第一定律。WSTG 4.7 是子项最庞大的大类，覆盖各类"输入被当指令执行"的漏洞。

## ① 是什么

**输入校验测试** 验证应用是否正确校验、过滤、转义用户输入，防止其被解释为代码/查询/指令。WSTG 4.7 含 XSS、HTTP 方法篡改、参数污染、SQL/命令/XXE 注入等。

**WSTG 类别码：`INPV`**

## ② 为什么重要

- 注入类常年居 OWASP Top 10 前列，可从数据泄露一路打到拿服务器权限。
- XSS 是钓鱼、盗 Cookie、蠕虫的起点；SQL 注入可直接拖库。

## ③ 核心概念拆解（对应 4.7.x）

- **反射/存储 XSS（4.7.1/2）**：脚本随响应执行；存储型危害更大（持久化）。
- **HTTP 动词篡改（4.7.3）**：把 GET 改 POST/PUT 绕过敏感操作限制。
- **HTTP 参数污染 HPP（4.7.4）**：同参数多次出现，看后端取哪个制造歧义。
- **SQL 注入（4.7.5）**：按数据库分（Oracle/MySQL/SQL Server/PostgreSQL/MS Access/NoSQL/ORM/Client-side），逐库验证报错/盲注/堆叠。
- **命令注入（4.7.6）**：`;`、`|`、`$()` 拼进系统命令。
- **XXE（4.7.7 类）**：XML 外部实体读文件/打内网（与进阶攻击呼应）。

## ④ 常见误区

- **误区 1**：页面不回显就安全。盲注靠延时/外带（OOB）判断。
- **误区 2**：只测一个参数。URL/Header/Cookie/JSON 字段都可能是注入点。
- **误区 3**：WAF 拦了就放弃。换编码/分块常能绕。

## ⑤ 一句话小结

输入校验测试把"用户输入"当"潜在指令"挑衅：改方法、重复参数、塞注入串——看是否被误执行。

*下一篇：错误处理测试（4.8）*

> 参考来源：*OWASP Web Security Testing Guide*（最新 5.0 开发线；上一稳定版 v4.2）（本文为原创讲解，非转载原文）
